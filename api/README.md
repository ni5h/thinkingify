# thinkingify api

Placeholder for the V2 backend.

V1 of the `ui` app is frontend-only (localStorage, no auth, no database). When
that needs to become multi-device or shared, this is where a FastAPI backend
would live. `ui/`'s `StorageService` is the only intended integration point —
see `ui/CLAUDE.md` for the migration plan.

Nothing here yet.
