export const environment = {
  production: true,
  // TODO: point at the deployed API once a hosting target is chosen
  // (Phase 1 deliberately leaves API hosting as an open item — see
  // ui/CLAUDE.md / api/README.md).
  apiBaseUrl: '',
  googleClientId: '',
};
